# nginx configuration

if PACKAGE_nginx

config NGINX_BACKTRACE
	bool "Log a backtrace on a server crash"
	default n

config NGINX_PCRE_JIT
	bool "Speedup regexps computations"
	default n

config NGINX_PCRE_UTF
	bool "Add support for UTF in regexps"
	default n

config NGINX_HTTP2
	bool "Enable support for HTTP/2"
	default y

endif
